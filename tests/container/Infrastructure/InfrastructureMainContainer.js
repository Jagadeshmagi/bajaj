import React from 'react';
import { shallow } from 'enzyme';
//import InfrastructureMainContainer from '../../../app/containers/Infrastructure/InfrastructureMainContainer';
import sinon from 'sinon';
import {expect} from 'chai';

describe('<InfrastructureMainContainer />', () => {
  it('renders the horizontal menu bar', () => {
    const spy = sinon.spy;
    //const wrapper = shallow(<InfrastructureMainContainer onItemClick={spy} activeTab='#/infrastructure/allresources' />);
    //const menuItems = wrapper.find('Link');
    //expect(menuItems).to.have.length.of(3);
    //buttons.at(0).simulate('click');
    //expect(buttons.at(0).prop('children')).to.equal('Sign In')
   });
});